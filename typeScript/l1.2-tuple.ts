/*
tuple-
    - defines types of Element
    - sequence of Element
    - number of elements
*/

let profile: [number, string, boolean];
// profile = ["no sequ", true, 101];//Sequence error, as it matters here

profile = [12, "Ninad", true];
console.log(profile);
